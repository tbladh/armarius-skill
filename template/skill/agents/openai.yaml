interface:
  display_name: "__PRODUCT_TITLE__"
  short_description: "Read and write documents, decks, spreadsheets, and image OCR with minimal repo-local tooling"
  default_prompt: "Use $__PRODUCT_NAME__ to sniff the needed setup, read the document, deck, spreadsheet, or image, and work from the extracted artifacts."
